# Daftar frontend

Next.js App Router frontend for Daftar.

```bash
npm install
npm run dev
```

The application proxies `/api/v1/*` to `DAFTAR_API_INTERNAL_URL`, which defaults
to `http://localhost:8080` locally and is configured as `http://api:8080` in Compose.
